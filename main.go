package main

import (
	TUI "main/src/pages/lobby"
)

func main() {
	TUI.Lobby()
	// box := tview.NewBox().SetBorder(true).SetTitle("| the sconds |")
	// if err := tview.NewApplication().SetRoot(box, true).Run(); err != nil {
	// 	panic(err)
	// }
}